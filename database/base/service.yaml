apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: db
    app.kubernetes.io/name: db
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app: db
  ports:
  - name: postgresql
    port: 5432
    targetPort: postgresql
    protocol: TCP
